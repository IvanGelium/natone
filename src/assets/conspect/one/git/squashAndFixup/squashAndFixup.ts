import { codeBlock } from '../../../../../utils'

const path = 'squashAndFixup'

const title = `Squash/Fixup`

const codeBase = `a`

const paragraph = `
${codeBlock(codeBase)}
`

export const squashAndFixupData = {
  path,
  title,
  content: paragraph,
  practice: null,
}
